import { Typography } from '@mui/material';
import SubLayout from '../pages/SubLayout';

const SalesManagement = () => {
  return (
    <SubLayout>
      <Typography variant="h2">영업 관리</Typography>
    </SubLayout>
  );
};

export default SalesManagement;
