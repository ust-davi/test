import { Typography } from '@mui/material';
import SubLayout from '../pages/SubLayout';

const Calculate = () => {
  return (
    <SubLayout>
      <Typography variant="h2">정산</Typography>
    </SubLayout>
  );
};

export default Calculate;
